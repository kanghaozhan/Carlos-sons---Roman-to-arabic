def roman_to_int(roman: str) -> int:

  # We specify the value of each letter
  values = {
      'I': 1,
      'V': 5,
      'X': 10,
      'L': 50,
      'C': 100,
      'D': 500,
      'M': 1000
  }
  """
  To avoid conflicts, we convert all letters
  to uppercase if they're not already
  """
  roman = roman.upper()
  total = 0  # This is the counter
  i = 0   # This variable will be used to indicate which letter

  # We create a loop to traverse the string (roman)
  while i < len(roman):
    current_value = values[roman[i]]

    # If we are not at the end, we look at the next symbol
    if i + 1 < len(roman):
      next_value = values[roman[i + 1]]

      if current_value < next_value:
        # Substraction case
        total += next_value - current_value
        i += 2   # We skip two symbols
        continue
    # Normal case: addition
    total += current_value
    i += 1

  return total

#Casos de prueba
print(roman_to_int("III"))
print(roman_to_int("XIX"))
print(roman_to_int("CCIC"))
print(roman_to_int("MDCLX"))
